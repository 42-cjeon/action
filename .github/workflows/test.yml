name: build

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: checkout
        uses: actions/checkout@v3
      - name: hello world
        uses: ./.github/actions/hello
        id: hw
      - name: Show output
        run: |
          echo "${{ steps.hw.outputs.time }}"
